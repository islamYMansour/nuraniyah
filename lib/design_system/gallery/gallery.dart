/// The design system's own preview harness.
///
/// Deliberately **not** exported from `design_system.dart`: the gallery is a
/// development tool, and app code has no reason to import it.
library;

export 'design_system_gallery.dart';
